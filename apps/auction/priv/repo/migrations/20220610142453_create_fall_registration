defmodule Auction.Repo.Migrations.CreateFallRegistration do
  use Ecto.Migration

  def change do
    create table(:fall_registrations) do
      add :student_id,  references(:students)
      add :class_id,    references(:classes)
      timestamps()
    end
   end
end
